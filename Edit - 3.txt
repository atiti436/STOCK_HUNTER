以下是我現在的需求，請依照需求改程式：

① 不要再把 technical['pass'] 當成硬條件剔除股票

guardian_4_technical() 回傳的內容請保留，但 不再作為強制篩選門檻。

技術面的訊號、均線方向、量能狀態，都只用來「加分或減分」，不要因為 pass = False 就把股票丟掉。

② 籌碼分數也不要當硬門檻

目前版本只有 preliminary_score >= 2 才會留下。
請改成：

只要基本條件通過（流動性、安全性 OK）
→ 不論分數是 0、1、-1… 都先留下來當候選

分數只是用來之後排序，不是“生死門檻”。

③ 保留的真正硬門檻只有兩種

價格安全門檻（避免價格太低）

流動性門檻（guardian_2）

只要這兩個有通過 → 先留下！

其餘全部只是加分項目，請不要在 quick_filter_stock() 內部 return None。

④ quick_filter_stock() 的回傳方式請改成「回傳所有資訊，不要丟掉股票」

請統一回傳這種格式（不論強弱都回傳）：

{
    "ticker": ticker,
    "basic": { ... },
    "chips": chips_info,
    "technical": technical_info,
    "day_trade": day_trade_info,
    "score": 綜合評分,  # 分數高低用來排序
}


不要因為條件不強就 return None。

⑤ 最後取 Top N 請在主流程做，而不是在 quick_filter 內做

流程改成：

quick_filter_stock() 回傳全部安全股（可能 200～400 支）

在主程式集中排序：

依 score 排序 → 取前 20 支做深度分析


AI 深度分析後，再做最終的推薦（例如 5–15 支）

⑥ 總流程示意（請依此改整個架構）
Step 1：快速篩選（但不剔除太多）

剔除：成交量過低、價格太低

其它全部保留（只是分數不同）

Step 2：排序

用綜合分數排序（籌碼 + 技術 + 當沖雷達）

Step 3：AI 深度分析前 20 檔
Step 4：AI 回傳後挑出最終推薦
⑦ 目標行為

完成後的系統應該要：

不會出現「1000 檔 → 0 檔」的極端情況

每天都能穩定給我約 20 檔候選

丟給 AI 做深度分析

最終給我 5〜15 檔不同屬性的推薦

✦ 備註：不用修改 guardian 的細節，只要改「使用方式」即可

所有監測器（guardian_x）都保持原樣，
但 不要再用 pass/fail 直接剔除股票。

改成：

fail = 扣分

pass = 加分

永遠回傳，不要 return None